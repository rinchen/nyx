[package]
name = "rcn"
version = "0.2.2"
edition = "2021"
rust-version = "1.85"
description = "Leveled Rust compressor (wire LZ, byte CM, hybrid BWT/CM) aiming to win ratio and compress speed vs each peer class"
license = "MIT"
readme = "README.md"
repository = "https://github.com/rinchen/rcn"

[dependencies]
clap = { version = "4", features = ["derive"] }
byteorder = "1"
crc32fast = "1"
thiserror = "1"
ans = "0.4.0"
divsufsort = "2"
mimalloc = "0.1"
rayon = "1"
libsais-rs = { version = "0.2.2", default-features = false, optional = true }

[features]
default = ["two_pass", "bwt_libsais"]
two_pass = []
no_avx2 = []
# Optional libsais SA-IS BWT backend (pure-Rust libsais-rs). Default on (W7).
bwt_libsais = ["dep:libsais-rs"]

[dev-dependencies]
criterion = "0.5"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"

# Symbol-friendly release build for macOS `sample` / Instruments (V3).
[profile.release-prof]
inherits = "release"
strip = "none"
debug = 1

[[bin]]
name = "rcn"
path = "src/main.rs"

[[bench]]
name = "throughput"
harness = false

[lints.clippy]
all = "warn"
pedantic = "warn"
nursery = "warn"
